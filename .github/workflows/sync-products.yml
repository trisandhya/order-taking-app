name: Sync Balaji Products
on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

jobs:
  update-json:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Download
        run: echo 'placeholder - real curl disabled in zip template'
      - name: Commit
        run: echo 'commit placeholder'
